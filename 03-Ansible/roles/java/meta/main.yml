---
galaxy_info:
  role_name: java
  author: Waleed Darwesh
  description: Installs OpenJDK and configures JAVA_HOME. Required by Jenkins and by the SonarQube scanner.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - java
    - jdk

dependencies:
  # Needs the APT cache refreshed and ca-certificates present.
  - role: common

---
galaxy_info:
  role_name: common
  author: Waleed Darwesh
  description: Baseline OS configuration — packages, timezone, sysctl, log retention, unattended security upgrades.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags:
    - system
    - baseline

# No dependencies — this role runs first and everything else builds on it.
dependencies: []

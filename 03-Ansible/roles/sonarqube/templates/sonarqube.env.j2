# ==============================================================================
# SonarQube secrets — managed by Ansible (roles/sonarqube), mode 0600
# ==============================================================================
# Rendered from Ansible Vault variables. Never commit this file, and never
# widen its permissions.
#
# Compose reads this automatically because it is named `.env` and sits beside
# docker-compose.yml.
# ==============================================================================

SONARQUBE_DB_PASSWORD={{ sonarqube_db_password }}
